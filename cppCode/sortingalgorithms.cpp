#include <vector> 
#include <iostream>



std::vector<int> mergesort(std::vector<int> incomingvector) {
    u_int length = incomingvector.size();
    std::vector<int> returnvector{};




    return returnvector;
}

main() {

    // declare a vector of doubles: 
    std::vector<double> dd; 

    // can be {} initialised: 
    std::vector<double> df{3.1, 2.3, 1.1}; // access elements also via []
    std::cout << df.size(); // prints 3 
}